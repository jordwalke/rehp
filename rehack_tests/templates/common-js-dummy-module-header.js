/**
 * ____CompilationUnitName
 * @providesModule ____CompilationUnitName
 */
"use strict";

/*
 * This is for use in dummy modules (like Sys_blocked_io) which have no real
 * runtime value, but are needed for module loaders to not fail.  Actually,
 * see the dummy js modules in runtime/rehack/js. They have to be created
 * once manually, but it's a one time deal.
 */

module.exports = {};
